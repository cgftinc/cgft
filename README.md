# cgft (archived)

> **This repository is archived.** Active development has moved to the monorepo.
>
> **Frozen at tag:** `pre-monorepo-vendor-2026-04-30` (2026-04-30)
>
> **Source of truth:** [`k8-deployment/cgft/`](https://github.com/castform-ai/cgft-k8-deployment/tree/master/cgft)

The `cgft` package (end-to-end pipeline for fine-tuning LLMs on RAG workloads) is now vendored into the `k8-deployment` monorepo. All future changes go there.

For the last working standalone snapshot, see the `pre-monorepo-vendor-2026-04-30` git tag.
